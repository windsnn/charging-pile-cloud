spring:
  application:
    name: gateway-service

  cloud:
    nacos:
      server-addr: 192.168.1.90:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: jwt-secret.yaml